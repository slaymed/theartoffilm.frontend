export enum SessionStatus {
    PAID = "paid",
    UNPAID = "unpaid",
    REFUNDED = "refunded",
}

export enum SessionType {
    GIFT = "gift",
    POSTER = "poster",
    ADVERTISEMENT = "advertisement",
}
